@model CreateLeaveAllocationVM

@{
    ViewData["Title"] = "Index";
    
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Leave Types
            </th>
            <th>
                Action
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.LeaveTypes) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td >
                @*@Html.ActionLink("Allocate To Employees", "SetLeave", new { id=item.Id, model = Model }, new { @class="btn btn-success" })*@
                <a class="btn btn-success" asp-action="SetLeave" asp-route-id ="@item.Id" asp-route-model="@Model"> <i class="fas fa-edit" aria-hidden="true"></i> Allocate To Employees</a>
            </td>
        </tr>
}
    </tbody>
</table>
